export enum ELanguageElement {
  KANJI = 'Kanji',
  VOCABULARY = 'Vocabulary',
  PARTICLES = 'Particles',
}

export enum EUri {
  KANJI = '/api/kanji/',
  VOCABULARY = '/api/vocabulary/',
  PARTICLES = '/api/grammar/particles/',
  KANA = '/api/kana/',
}

export enum ESnackbarVariant {
  SUCCESS = 'alert-success',
  INFO = 'alert-info',
  ERROR = 'alert-error',
  WARNING = 'alert-warning',
}
